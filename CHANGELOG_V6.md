# V6 — Channel Requests First

## تغییرات اصلی

- داشبورد مستقل برای **کل Pending کانال** با `ChannelFull.requests_pending`.
- Random Approve از **کل صف کانال** برای Owner، با Reservoir Sampling روی تمام صفحات درخواست‌ها.
- تأیید همه درخواست‌های کل کانال برای Owner.
- تفکیک کامل «مدیریت درخواست‌های کانال» از «مدیریت Invite Linkها».
- محدودیت Owner به‌صورت واضح: Admin معمولی همچنان عدد کل Pending را می‌بیند، اما V6 برای صف لینک‌های سایر ادمین‌ها دسترسی جعلی نشان نمی‌دهد.
- Invite Link Manager با لینک Approval و Direct.
- تنظیم عنوان، تاریخ انقضا، محدودیت ورود مستقیم، Revoke و ویرایش عنوان/انقضا.
- Auto Approve مستقل برای هر Approval Link، قابل تنظیم هنگام ساخت یا بعداً.
- گزارش یک‌دقیقه‌ای درخواست‌های **جدید** برای هر لینک، فقط وقتی در آن دقیقه Event جدیدی رسیده باشد.
- OCR اعداد برای ورودی‌های مدیریتی با مرحله تأیید قبل از عملیات.
- QR Login و `auth.enc` نسخه V5/V5.1 حفظ شده‌اند و نیاز به Login مجدد نیست.
- محافظت از Pagination صف‌های بزرگ در برابر تکرار Offset و FloodWait.
